<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="main_screen_fragments.Appareil"

    android:background="@drawable/moun">






    <LinearLayout
        android:id="@+id/buttons_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginHorizontal="35dp">

        <ImageView
            android:id="@+id/casque_connected"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:src="@drawable/ic_connected_casque"
            android:layout_marginBottom="25dp"
            app:tint="@color/green_settings"
            tools:ignore="contentDescription" />


        <Button
            android:id="@+id/connectButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/CustomAppThemeGreen"
            android:background="@drawable/button_green"
            android:text="casque connecté"
            />
        <Button
            android:id="@+id/disconnectButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/CustomAppThemeRed"
            android:background="@drawable/button_red"
            android:layout_marginTop="26sp"
            android:text="deconnecter"
            />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>